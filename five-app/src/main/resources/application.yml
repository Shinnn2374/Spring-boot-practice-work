spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/bookdb
    username: root
    password: 603429Daxter
  jpa:
    hibernate:
      ddl-auto: update
  cache:
    type: redis
    redis:
      time-to-live: 60000
  data:
    redis:
      host: localhost
      port: 6379

